{
  "buildCommand": "pnpm run build && npx expo export --platform web",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/**/*.ts": {
      "runtime": "@vercel/node@5.5.16"
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/dist/$1" }
  ]
}
