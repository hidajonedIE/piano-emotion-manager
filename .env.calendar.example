# ============================================================================
# Calendar Sync Environment Variables
# ============================================================================

# Encryption Key (generate with: npx tsx scripts/generate-calendar-key.ts)
CALENDAR_ENCRYPTION_KEY=your_64_character_hex_key_here

# Google Calendar OAuth
GOOGLE_CALENDAR_CLIENT_ID=your_google_client_id
GOOGLE_CALENDAR_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3000/api/calendar/google/callback

# Microsoft Calendar OAuth
MICROSOFT_CALENDAR_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CALENDAR_CLIENT_SECRET=your_microsoft_client_secret
MICROSOFT_CALENDAR_REDIRECT_URI=http://localhost:3000/api/calendar/microsoft/callback
MICROSOFT_TENANT_ID=common

# Webhook URL (use ngrok for local development)
CALENDAR_WEBHOOK_URL=https://your-domain.com/api/calendar/webhook

# ============================================================================
# Setup Instructions
# ============================================================================

# 1. Generate encryption key:
#    npx tsx scripts/generate-calendar-key.ts

# 2. Setup Google Calendar API:
#    - Go to: https://console.cloud.google.com/
#    - Create a new project or select existing
#    - Enable Google Calendar API
#    - Create OAuth 2.0 credentials (Web application)
#    - Add authorized redirect URI: http://localhost:3000/api/calendar/google/callback
#    - Copy Client ID and Client Secret

# 3. Setup Microsoft Graph API:
#    - Go to: https://portal.azure.com/
#    - Navigate to Azure Active Directory > App registrations
#    - Create a new registration
#    - Add redirect URI: http://localhost:3000/api/calendar/microsoft/callback
#    - Create a client secret
#    - Copy Application (client) ID and Client Secret
#    - Grant API permissions: Calendars.ReadWrite, offline_access, User.Read

# 4. Setup Webhook URL (for local development):
#    - Install ngrok: https://ngrok.com/
#    - Run: ngrok http 3000
#    - Copy the HTTPS URL and set as CALENDAR_WEBHOOK_URL
#    - Add /google or /microsoft to the end for each provider

# 5. Copy this file to .env and fill in your values:
#    cp .env.calendar.example .env.local
#    # Then edit .env.local with your actual values
